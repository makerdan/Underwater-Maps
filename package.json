{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "postinstall": "pnpm --filter @workspace/api-spec run codegen:generate",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "check:codegen": "pnpm --filter @workspace/api-spec run check:codegen",
    "check:codegen-stale": "pnpm --filter @workspace/api-spec run codegen:generate && git diff --quiet lib/api-client-react/src/generated lib/api-zod/src/generated || (echo 'ERROR: Generated API client is stale. Run codegen:generate and commit the result.' && exit 1)",
    "typecheck": "pnpm --filter @workspace/api-spec run codegen:generate && pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "lint": "eslint artifacts/bathyscan/src artifacts/api-server/src tests/e2e",
    "test:unit": "pnpm -r --if-present run test:unit",
    "test:e2e": "playwright test",
    "capture:help-screenshots": "node scripts/capture-help-screenshots.mjs",
    "docs": "node scripts/generate-api-docs.mjs",
    "check:docs-stale": "node scripts/generate-api-docs.mjs --check",
    "test-all": "pnpm run typecheck && pnpm run lint && pnpm run test:unit && pnpm run check:docs-stale",
    "audits:status": "node scripts/audits-status.mjs"
  },
  "private": true,
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@typescript-eslint/eslint-plugin": "^8.59.4",
    "@typescript-eslint/parser": "^8.59.4",
    "eslint": "^10.4.0",
    "eslint-plugin-react": "^7.37.5",
    "eslint-plugin-react-hooks": "^7.1.1",
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
